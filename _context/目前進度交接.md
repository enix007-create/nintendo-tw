# 任天堂台灣遊戲監看 — 進度交接（2026-06-01）

下一個對話的 Claude，請先讀完這份再開始工作。

---

## 一句話總結

自動爬任天堂台灣 Switch / Switch 2 遊戲資訊的網站，已部署到 GitHub Pages（repo `enix007-create/nintendo-tw`），每天更新一次（台灣 0 點排程，見「CI Workflow 行為」）。**v2 廣域爬蟲已上線，目前產出 248 款（特價 40）**，比 v1 的 70 款多了 3.5 倍。後續靠 hourly refresh 慢慢累積 name/cover 到上千款。

---

## 設計原則（使用者確認過，重要）

| 欄位 | 來源 | 信任度 |
|---|---|---|
| **「是否在 TW eshop 上架」** | TW Price API（`api.ec.nintendo.com/v1/price?country=TW`）回傳非 `not_found` 才算 | 硬條件，必須 TW |
| **價格** | 同上，TWD `regular_price` / `discount_price` | 必須 TW |
| **名稱** | catalog 抓到 → TW；catalog 沒抓到 → HK 來源（可能用字略不同） | TW 優先，HK fallback OK |
| **封面圖** | catalog → Contentful TW 圖；HK fallback → HK store 產品圖 | TW 優先，HK fallback OK |

使用者明確說「**遊戲確實在台灣 eshop 上架是必要的，圖片名稱從 HK 拿沒問題**」。

---

## v2 架構（重點）

### 四階段獨立執行 + 持久化 cache

```
scrape.py --phase=catalog    # Playwright 渲染 Switch + Switch 2 目錄頁
scrape.py --phase=scan       # Price API NSUID 範圍掃描（50000–135000）
scrape.py --phase=tw_search  # ★ TW 官方搜尋端點 fan-out（267 keywords，TW 權威來源）
scrape.py --phase=names      # 缺名稱者去 HK store / ec.nintendo.com 補抓（fallback）
scrape.py --phase=build      # Price API 重打、合併、輸出 web/games.json

# 包裝：
scrape.py --phase=refresh    # catalog + tw_search + names(limit=300) + build。hourly
scrape.py --phase=bootstrap  # catalog + scan + tw_search + names(limit=500) + build。daily
```

### Cache 持久化在 `scraper/_cache/`

- `games_meta.json`：核心 cache，存所有已知 NSUID 的 `{platform, name, cover_url, ...}`。每次 commit 進 repo，跨 run 累積
- `scan_nsuids.json`：scan phase 的 raw prices（build phase 拿來當 Price API 被擋時的 fallback）
- `catalog.json`：catalog phase 摘要

### 重要技術細節

1. **Akamai 反爬**：`api.ec.nintendo.com` 對高並行/高頻率超敏感。設定 `SCAN_CONCURRENCY=4`、`SCAN_DELAY=0.25s`、`SCAN_MAX_RETRY=2`、bootstrap 跑完 scan 後 sleep 30s 退火再跑 names
2. **TW 跟 HK NSUID 不共用**（2026-06-01 驗證）：同一款 FF XII THE ZODIAC AGE，TW=`70010000103647`、HK=`70010000016505`，且 cross-region Price API 都回 `not_found`。所以 HK store fallback 只對「兩地剛好共用 NSUID」的款有效；其他款必須靠 TW 自有來源（catalog / tw_search）拿名字
3. **HK 202 retry 沒用**（2026-06-01 驗證）：實測 `202_retry_hit=0/300`，所有 202 都不是 async 生成中，純粹是 HK 沒有那個 NSUID。retry 已從 `_fetch_hk_one` 移除
4. **TW search 上限**：`nintendo.com/tw/search/software?k=<keyword>` 每查詢回上限 ~24 筆，且 `page=2` 無效（會回 page=1 同樣結果）。所以必須用 keyword fan-out 才能擴大覆蓋面
5. **Build fallback**：因為 Price API 在 build 階段常被 403，build 改成先 try Price API refresh，失敗者用 scan cache 的 prices 補上。**這就是為什麼即使 refresh 0 筆，最終還能輸出 248 款**
6. **Push race**：bot commit 跟外部 push 衝突時，workflow 用 `git fetch + reset --hard origin/main + 把產出貼回` 而非 rebase，保證 fast-forward

---

## 目前產出（2026-06-01 bootstrap 跑完後）

- **Meta cache：~2940 個 NSUID**（catalog 79 + scan 累積 ~2796）
- **games.json：248 款，特價 40 款**
  - 大部份有 name 的來自 catalog（79 款）+ 第一次 names phase 補到的（95 款）
  - 剩下 ~74 款是怎麼來的：scan + HK fallback 累積（多次 run）
- **缺口**：~2700 個 NSUID 在 meta 裡但沒 name（HK 那邊 202/403 拿不到），所以沒進 games.json

---

## 已知問題 / 下一步候選

### 已知問題

1. ~~**HK store 202 沒重試**~~（試過後拔掉 2026-06-01）
   - 加 retry 後實測命中 0/300 → 確認 202 不是 async 占位，是 HK 沒有那 NSUID
   - 已從 `_fetch_hk_one` 移除 retry 邏輯
2. ~~**TW 官方資料源缺口**~~（2026-06-01 加 phase_tw_search）
   - 新增 `phase_tw_search`：用 `bilingual_dict.SERIES` + 中英常用詞 + indie publisher + 中文 2-gram 共 **393 個 keyword**
   - 打 `nintendo.com/tw/search/software?k=<kw>` 取嵌入 JSON
   - 每筆有 `title` / `nsuid` / `imageHero.url` / `hardwareCategory`
   - 首輪 CI 跑 267 keyword 41s → 579 unique NSUID（113 新）、補 name 505、cover 545、platform 113
   - 串入 run_refresh / run_bootstrap，順序：catalog → (scan) → tw_search → names(HK fallback) → build
3. ~~**title parser bug**~~（2026-06-01 修）
   - 原本 regex `(?:[^"\\]|\\.)*?` 解碼帶 escaped quote 的 title（例如 `"Run for Money": Hunter VS Runner!`）會誤判結束標記，只擷取到 `\\`
   - 改用 `_tw_scan_inner_string` char-by-char scanner，正確追蹤 inner JSON escape state
   - 加 `_is_junk_name` guard 自動覆寫既有髒資料（單 backslash / 純符號），下一輪 CI 自動修
4. **缺口分析**（2026-06-01）
   - meta 中 1985 個 NSUID 仍無 name（hk_checked=True 都試過了）
   - 缺口集中在 NSUID 100000–119999（54%），即近 1–2 年新發行的長尾 indie
   - 12 樣本對 100 個擴張 keyword 命中率 3/12 = 25%；多數剩餘是泛用詞無法覆蓋的 indie
5. ~~**卡片連結常 404**~~（2026-06-01 修）
   - 舊 store_url = `store.nintendo.com.hk/{NSUID}` 對非兩地共用 NSUID 都 404
   - 新邏輯三段 fallback：
     - `detail_url`：先用 catalog 來源 → 然後用 tw_search 抓到的 `pageLinkCustom`（多為 Switch 2 + 部分 Switch，會構造 `nintendo.com/tw/{games/switch2|switch}/<softCode>/`）
     - `store_url`：改用 `ec.nintendo.com/HK/zh/titles/{NSUID}`（covers 多很多 NSUID，包括使用者舉的 73501）
     - `search_url`：永遠生效的 `nintendo.com/tw/search/software?k=<encoded name>`
   - 前端 `index.html` 點擊處理改成 `detail_url || store_url || search_url`

2. **catalog 79 款裡有 5 款沒 name**（低優先）
   - NSUID: 70010000096069, 70010000121085, 70010000127209 等
   - catalog 抓不到 alt/text 名稱（卡片版型異常），HK 也沒查到

3. **Cover 命中率低**（中優先）
   - 248 款裡只有 ~79 款（catalog 抓到的）有封面
   - HK store 202 拿不到圖。要嘛 retry，要嘛找新來源（ec.nintendo.com 的 og:image？Contentful pattern？）

4. **CI 跑久了會被 Akamai 鎖**
   - 連續觸發 Run workflow 可能 IP 被 ban
   - 通常等 15-30 分鐘可恢復。real schedule（每小時跑）IP 會輪換所以不太影響

### 下一步候選

| 任務 | 預估效果 | 優先 |
|---|---|---|
| 202 retry + UI cover fallback | names 命中率 19%→70%，前端缺圖也不爆 | 高 |
| 等 hourly 自然累積 | 12-24 小時內 names 數從 ~170 補到 1000+ | 中 |
| TW 詳情頁抓 TW 中文名 | 名稱精準度更高，但要多一輪 HTTP | 低 |
| 加更多中英系列字典 | 搜尋體驗 | 低 |
| 新上架 / 新特價 diff 提醒 | 額外功能 | 低 |

---

## UI 變更紀錄（2026-06-04）

新增「已購買」標記功能（`web/index.html`）：

- **localStorage key**：`nintendo_tw_owned_v1`（獨立於收藏 `nintendo_tw_favs_v1`）
- **卡片 UI**：收藏 ❤️ 左邊新增已購買按鈕（🛒 ↔ ✅）；已購買卡片整張 `opacity: .55`、hover 還原為 1，info 區顯示綠底「已購買」tag
- **filter chips**：新增「只看已購買」「排除已購買」兩個 chip，兩者互斥（點一個會自動關掉另一個）
- **匯出 / 匯入**：新增「匯出已購買」「匯入已購買」按鈕，沿用同一個 modal；`modalMode` 改為 `export-fav` / `import-fav` / `export-owned` / `import-owned`，匯入皆為合併到現有清單
- 已通過 `node --check` 語法檢查

## 檔案結構

```
nintendo-tw/
├── 執行計畫.html              v2 設計，部分內容已過時
├── 資料來源筆記.html
├── 部署說明.html
├── _context/
│   └── 目前進度交接.md        ← 你正在讀的這份
├── scraper/
│   ├── scrape.py             ★ v2 主爬蟲，四階段 + cache
│   ├── bilingual_dict.py     60+ 組中英系列字典
│   ├── requirements.txt
│   └── _cache/
│       ├── games_meta.json   ★ 核心 cache，跨 run 累積
│       ├── scan_nsuids.json  scan phase raw prices
│       └── catalog.json
├── web/
│   ├── index.html            前端
│   └── games.json            ★ 爬蟲產出
├── .github/workflows/
│   └── hourly-scrape.yml     ★ 每天一次 bootstrap（cron 0 16 UTC = 台灣 0 點）
└── .gitignore
```

---

## 跟使用者互動的注意事項

- 使用繁體中文
- 簡潔直接，不多餘解釋
- 遇到不確定先問，不要猜
- 覆蓋檔案前先確認
- 重要設定先讓使用者看內容
- **給 git push 指令前，務必先給 `git pull`**：每日 CI 會把產出（games.json / _cache/*）commit 回 main，使用者本地若沒先 pull 就 push 會被 reject。固定順序：`git pull --rebase` → `git add` → `git commit` → `git push`
- **每次改動都要同步更新本交接文件**（使用者要求，2026-07-01）

---

## CI Workflow 行為（2026-07-01 更新）

`.github/workflows/hourly-scrape.yml`（檔名保留舊名，實際是每天一次）：

- `cron: 0 16 * * *`：每天 16:00 UTC = **台灣時間 00:00（隔天）**觸發。選台灣 0 點是為了抓當天午夜剛上線的新特價。
- **注意 GitHub 排程延遲**：scheduled 實際執行常比排定時間晚 1–3 小時（舊設定排 03:05 UTC 實際都約 06–07 UTC 才跑）。所以實際更新有機會落在台灣 0–3 點，不保證嚴格在 2 點前。改 cron 無法完全控制。
- **每次都跑 `--phase=bootstrap`**（完整掃描），沒有 hourly/refresh 分流。
- `workflow_dispatch` 手動觸發 → 一樣跑 bootstrap
- `push:` 也會觸發（路徑：`scraper/**`、`web/**` 或 `.github/workflows/**`）→ bootstrap
- timeout 25 分鐘
- Commit 階段失敗也跑（`if: always()`）— 保住部分 cache
- Push 衝突時 `git reset --hard origin/main + 把產出貼回` 重試 4 次
- 失敗會 email 到 `enix_007@yahoo.com.tw`

### 每日一次的取捨（重要）

因為一天只跑一次，**新特價最慢要等到隔天排程才會出現在網站上**。例：緋夜傳奇 Remastered 的台灣特價 2026-06-30 16:00 UTC（台灣 7/1 00:00）上線，但當天最後一次爬蟲跑在 06-30 07:00 UTC（特價開始前 9 小時），所以那批資料抓不到折扣是正常的。使用者已確認接受「每天更新一次」，把排程移到台灣 0 點就是為了縮短這個延遲。

---

## 環境 / 注意

- 工作資料夾：`C:\Users\user\Desktop\Claude-workspace\projects\nintendo-tw`（Linux 端 `/sessions/<session>/mnt/nintendo-tw/`）
- bash 沙箱 45 秒限時。catalog phase 需要 ~30s，scan 本機跑會被 Akamai 鎖，**本機驗證困難，靠 CI 跑**
- 沙箱 git 對 `.git` 寫權限不全，commit/push 由使用者在 Windows 端執行
- pip 用 `--break-system-packages`，Playwright 用 chromium

---

## 立刻可以做的接續任務（按優先）

1. ~~加 202 retry~~（done 2026-06-01，後來驗證沒效，已拔）
2. ~~前端 cover fallback~~（done 2026-06-01）— 無 `cover_url` → 平台色漸層底 + 遊戲名置中白字
3. ~~TW 詳情頁/搜尋頁拿 TW 中文名~~（done 2026-06-01，phase_tw_search 上線）
4. ~~擴充 keyword 字典~~（done 2026-06-01，267 → 393）
5. ~~修 title parser bug~~（done 2026-06-01）
6. **觀察下次 CI 跑完數字**：估計 tw_search 從 579 NSUID 進一步上升到 700–800，games.json 突破 800 款
7. **若還想推高覆蓋率**：indie publisher / 泛用動詞 / 顏色動物字眼。邊際 ROI 約 200 keyword → 200–300 個新 NSUID
8. （可選）破圖處理：cover_url 存在但 404 時 fallback，需把 background-image 改成 `<img onerror>`
9. （可選）新上架 / 新特價 diff 提醒
